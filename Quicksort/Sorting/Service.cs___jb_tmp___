using System;

namespace Sorting
{
    public class Service
    {
        public static void sortWith(int[] array, string sorting)
        {
            object[] arguments = {array};
            Type.GetType("Sorting.Sorting").GetMethod(sorting).Invoke(null, arguments);
        }
    }
}